# PhonePadBridge.WebBridge

WPF + ASP.NET Core local bridge app.

## Scripts

```bat
run_from_source.bat
build_single_exe.bat
```

## Important

The published EXE still needs external system dependencies:

- ViGEmBus driver for virtual Xbox output
- Android Platform Tools / `adb.exe` for Android USB mode
