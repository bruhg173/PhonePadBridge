<Application x:Class="PhonePadBridge.WebBridge.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Color x:Key="ColorBg0">#050814</Color>
        <Color x:Key="ColorBg1">#090F22</Color>
        <Color x:Key="ColorBg2">#101B35</Color>
        <Color x:Key="ColorPanel">#111A2F</Color>
        <Color x:Key="ColorPanelDeep">#07101F</Color>
        <Color x:Key="ColorLine">#223553</Color>
        <Color x:Key="ColorAccent">#37D7FF</Color>
        <Color x:Key="ColorAccent2">#9A5CFF</Color>
        <Color x:Key="ColorHot">#FF4DA6</Color>
        <Color x:Key="ColorGood">#48F2A5</Color>

        <SolidColorBrush x:Key="Bg" Color="{StaticResource ColorBg0}"/>
        <SolidColorBrush x:Key="Panel" Color="{StaticResource ColorPanel}"/>
        <SolidColorBrush x:Key="Panel2" Color="{StaticResource ColorPanelDeep}"/>
        <SolidColorBrush x:Key="Accent" Color="{StaticResource ColorAccent}"/>
        <SolidColorBrush x:Key="Accent2" Color="{StaticResource ColorAccent2}"/>
        <SolidColorBrush x:Key="Hot" Color="{StaticResource ColorHot}"/>
        <SolidColorBrush x:Key="Good" Color="{StaticResource ColorGood}"/>
        <SolidColorBrush x:Key="Text" Color="#F8FBFF"/>
        <SolidColorBrush x:Key="Muted" Color="#91A0BE"/>
        <SolidColorBrush x:Key="Line" Color="{StaticResource ColorLine}"/>

        <DropShadowEffect x:Key="PanelShadow" BlurRadius="30" ShadowDepth="10" Opacity="0.35" Color="#000000"/>
        <DropShadowEffect x:Key="CyanGlow" BlurRadius="22" ShadowDepth="0" Opacity="0.70" Color="#37D7FF"/>
        <DropShadowEffect x:Key="PinkGlow" BlurRadius="18" ShadowDepth="0" Opacity="0.55" Color="#FF4DA6"/>

        <LinearGradientBrush x:Key="PrimaryButtonBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#1A6C8E" Offset="0"/>
            <GradientStop Color="#164B63" Offset="0.55"/>
            <GradientStop Color="#22345C" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="DarkCardBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#142139" Offset="0"/>
            <GradientStop Color="#0A1122" Offset="1"/>
        </LinearGradientBrush>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}"/>
            <Setter Property="BorderBrush" Value="#55DFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16">
                            <Grid>
                                <Border CornerRadius="15" Opacity="0.22">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#FFFFFF" Offset="0"/>
                                            <GradientStop Color="#00FFFFFF" Offset="0.75"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Effect" Value="{StaticResource CyanGlow}"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="#8EEBFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#55213F" Offset="0"/>
                        <GradientStop Color="#2A122B" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#FF4DA6"/>
        </Style>

        <Style x:Key="GreenButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#164A3D" Offset="0"/>
                        <GradientStop Color="#132B35" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#48F2A5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource Text}"/>
            <Setter Property="Background" Value="#0A1223"/>
            <Setter Property="BorderBrush" Value="#2A4166"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="CaretBrush" Value="#37D7FF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Foreground" Value="{StaticResource Accent}"/>
            <Setter Property="Background" Value="#08111F"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="#253A5F"
                                BorderThickness="1"
                                CornerRadius="9">
                            <Grid ClipToBounds="True">
                                <Border x:Name="PART_Track" CornerRadius="8"/>
                                <Border x:Name="PART_Indicator"
                                        HorizontalAlignment="Left"
                                        CornerRadius="8">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#37D7FF" Offset="0"/>
                                            <GradientStop Color="#9A5CFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
